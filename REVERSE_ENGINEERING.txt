OBD11 & OBD11 VAG Bypass — runtime targets

Regular app: com.voltasit.obdeleven.ios.basic / 1.11.0
VAG app: com.voltasit.obdeleven.ios / 1.9.73 / build 1785335496
VAG executable: OBDeleven VAG, arm64, LC_ENCRYPTION_INFO_64 cryptid=0.

VAG static analysis (main-image offsets, not runtime patches):
- 0x331E7C: force-update completion path reads CFBundleVersion using
  helper 0x15E30C (NSBundle.mainBundle objectForInfoDictionaryKey:).
- The required integer is obtained using remote-config key ios_version.
- 0x3320BC / 0x3320C0: signed comparison of installed build vs required build;
  completion receives currentBuild < requiredBuild.
- 0x39DBAC: currentAppVersion reads NSBundle.infoDictionary and extracts
  CFBundleShortVersionString. Its failure log names AppVersionUtility.swift.
- Header keys x-mobile-app-version (0x1262CF0) and x-mobile-app-build
  (0x1262D10) appear in the request-header enum code.

Runtime hooks:
- NSBundle objectForInfoDictionaryKey: and infoDictionary; main bundle only.
- CFBundleGetValueForInfoDictionaryKey; main bundle only.
- NSURLSession dataTaskWithRequest: (+ completion variant) and
  uploadTaskWithRequest:fromData:completionHandler:.
- Both apps report their selected short version and build 2147483647.
- Outgoing x-mobile-app-version uses that app's selected short version.
- VAG additionally reports x-mobile-app-build=2147483647.
- No binary offsets, startup-state overrides, firmware checks or vehicle
  communication routines are patched.

Preferences:
- Regular: enabled / spoofedVersion. VAG: vagEnabled / vagSpoofedVersion.
- Shared rootless preference domain com.551.obdelevenupdatebypass.
- libSandy grants both app bundle IDs read access to the preferences file.
- Off or a selected version equal to the actual version disables every spoof
  in that process. Fully reopen apps to clear versions cached by their code.
- Regular preset: 2.10.0. VAG preset: 1.9.99 (a synthetic spoof value,
  not a claim that this is a released VAG version).

Static analysis supports these hook targets. Live VAG startup and server
acceptance must be verified on an iOS device.
